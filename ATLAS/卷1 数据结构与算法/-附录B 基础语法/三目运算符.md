---
tags:
  - 算法
---
# 语法

三目运算符可以用于处理一些二元判断问题。

语法：

```cpp
[条件] ? [结果1] : [结果2]
```

在这个语句中，如果条件为真，则表达式的结果为 结果1；否则为 结果2 。

# 题目

【NCEPU集训】求最大值
https://www.luogu.com.cn/problem/U716708

# min 和 max 函数

用于返回输入数据中的最小值和最大值。

语法：

```cpp
int mx = max(a,b);
```