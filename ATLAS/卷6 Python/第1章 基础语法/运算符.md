---
tags:
  - Python
---
# 1. 算术运算

加减乘除和取模和C++一样，不过这里的除法得到的是浮点类型。

`**` ：幂次运算。
`//` ：整除，向下取整。

# 2. 比较运算

比较运算符和C++一样。

# 3. 赋值运算

和其他运算赋值运算符一样，可以通过使用 `**=` 和 `//=` 来进行幂赋值和取整除赋值。

# 4. 逻辑运算

在Python中使用 `and` ，`or` 和 `not` 来进行与，或和非逻辑的运算。

# 5. 成员运算

Python支持通过成员运算符 `in` 和 `not in` 判断元素是否在指定序列中。
