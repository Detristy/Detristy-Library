---
tags:
  - Python
---
# 1. 函数的定义
使用 `def` 来定义一个函数，使用 `return [表达式]` 结束函数，不带表达式的 return 相当于返回 None 。

定义语法：
```python
def functionName(Parameters):
	body
```

body 是函数体，parameters 是传入的参数。
# 2. 函数调用
在完成对函数的定义之后，可以通过另一个函数调用执行。

